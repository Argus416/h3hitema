import { Box, Button, Container } from "@mui/material";
import Calendar from "../components/Calendar";

const Home = () => {
	return (
		<Container>
			<Calendar />
		</Container>
	);
};

export default Home;
